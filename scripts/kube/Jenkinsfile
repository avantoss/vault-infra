library identifier: 'jenkins-global-lib'

kube().pod( "kubernetes", "kubernetes-vault-test" ).
    withServiceAccount( "jenkins" ).
node {
    stage('secret') {
        String luggage = vault().secret( "secret/data/luggage" )
        echo "Got secret: ${luggage}"
    }
}
